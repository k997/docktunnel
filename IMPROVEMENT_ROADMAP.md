# DockTunnel 改进路线图实施策略

## 范围与原则
- 本文基于既定改进路线图，给出每个阶段的具体实现策略。
- 目标优先级：`正确性 > 一致性 > 可观测性 > 性能`。
- 变更策略：先补测试与开关，再逐步切流，避免一次性高风险改动。

## 阶段一（P0，1-2 周）：可靠性主链

### 目标
- 保证容器生命周期事件最终都能收敛到正确隧道状态。

### 实现策略
1. 事件处理链路补全
- 统一处理 `start/stop/die` 所需上下文，避免 stop/die 因缺少容器标签被跳过。
- 事件对象中补充最小可判定信息（容器 ID、标签快照或可回查信息）。

2. 双轨收敛机制
- 保留事件驱动的快速路径。
- 增加固定周期全量对账（reconcile）作为兜底路径，修复漏事件造成的漂移。

3. 幂等化要求
- 所有“添加/删除规则、DNS upsert/delete”都按幂等设计执行，重复调用不产生副作用。

4. 退出行为分级
- 定义退出策略：`fast-exit`（快速退出）和 `graceful-cleanup`（尽力清理）。
- 清理流程使用独立超时上下文，不复用已取消上下文。

### 测试与验收
- 场景：连续高频启停、事件丢失模拟、进程重启后自动收敛。
- 验收指标：
  - 停止容器后在约定窗口内（例如 30-60s）状态收敛。
  - 无永久残留 DNS/Ingress 记录。
  - 高并发事件下无死锁、无 goroutine 泄漏。

### 风险与回滚
- 风险：对账频率过高导致 API 压力上升。
- 控制：对账周期可配置，配合速率限制与指数退避。
- 回滚：对账路径支持 feature flag，一键关闭只保留事件路径。

## 阶段二（P0/P1，1 周）：Retention 语义统一

### 目标
- `Immediate/Timed/Forever` 在 Ingress、DNS、State 三层行为一致且可预期。

### 实现策略
1. 建立“资源行为矩阵”
- 定义每种策略在三层资源的预期动作（保留/删除/延后删除）。
- 作为实现与测试唯一判定依据。

2. 统一状态机
- 设计路由生命周期状态：`Active -> PendingDelete -> Deleted`。
- 所有删除动作从状态机触发，避免多个模块各自判断导致语义分叉。

3. 同步逻辑对齐
- DNS 期望集合不只依赖运行中容器，还需纳入保留态路由。
- 清理动作以状态机输出为准，不直接推导。

### 测试与验收
- 覆盖三种 retention 策略的全流程测试。
- 验收指标：
  - 策略行为与文档矩阵 100% 一致。
  - 无“Ingress 还在但 DNS 已删”或反向不一致。

### 风险与回滚
- 风险：历史行为变化影响用户预期。
- 控制：提供版本说明与迁移说明，必要时加兼容模式。
- 回滚：保留旧策略计算路径，按配置切换。

## 阶段三（P1，1-2 周）：状态模型升级（容器+服务）

### 目标
- 解决多服务容器被覆盖，保证每个 service 独立生命周期管理。

### 实现策略
1. 主键模型调整
- 将状态主键由 `containerID` 升级为 `containerID + serviceName`。
- 引入聚合视图用于容器级查询，避免调用方改动过大。

2. 存储与迁移
- 设计状态文件版本升级策略。
- 旧版本加载时执行一次性迁移，并输出迁移日志摘要。

3. GC 与恢复按 service 维度执行
- 过期删除、重启恢复都按 service 粒度处理。
- 容器级操作由聚合层完成，而非覆盖单条 service 记录。

### 测试与验收
- 新增多服务容器完整生命周期测试：
  - 多服务同时进入 retention。
  - 部分服务恢复，部分服务过期删除。
- 验收指标：
  - 任意服务状态变更不影响同容器其他服务。
  - 重启后多服务状态完整恢复。

### 风险与回滚
- 风险：状态迁移失败导致启动异常。
- 控制：迁移失败自动降级为空状态并告警；保留备份文件。
- 回滚：支持读取旧版本快照并进入只读兼容模式。

## 阶段四（P1，1 周）：失败补偿与重试治理

### 目标
- 外部 API 失败可观测、可重试、可补偿，最终一致性可恢复。

### 实现策略
1. 失败分类
- 错误分为：可重试（429/5xx/超时）与不可重试（鉴权/参数错误）。
- 不可重试立即上报并中止重试。

2. 补偿队列
- 对失败变更记录“期望动作 + 重试次数 + 最后错误 + 下次执行时间”。
- 后台任务按退避策略重放，成功后出队。

3. 幂等重放
- Upsert/Delete 保证重复执行安全，防止补偿重放造成副作用。

### 测试与验收
- 故障注入：429、5xx、网络抖动、鉴权失败。
- 验收指标：
  - 可重试错误在上限内恢复。
  - 不可重试错误可快速暴露并可定位。
  - 队列积压、失败率、重试耗时可观测。

### 风险与回滚
- 风险：补偿队列无限积压。
- 控制：设置队列上限、死信策略和告警阈值。
- 回滚：可禁用补偿，仅保留同步失败直报。

## 阶段五（P1/P2，1 周）：持久化闭环

### 目标
- 进程重启后状态连续，避免 retention/flapping 信息丢失。

### 实现策略
1. 保存策略三层化
- 周期保存：固定间隔保存脏状态。
- 关键事件保存：状态机关键跃迁后触发保存。
- 退出前强制保存：优雅退出路径落盘一次。

2. 原子写与容错
- 继续使用临时文件 + rename 原子替换。
- 保留最近 N 份备份，主文件损坏时可回退。

3. 数据一致性校验
- 启动加载后做基本校验（版本、必填字段、时间戳合法性）。
- 校验失败进入降级并打印诊断摘要。

### 测试与验收
- 场景：异常退出、文件损坏、版本升级回读。
- 验收指标：
  - 重启后关键状态恢复成功率接近 100%。
  - 坏文件不阻塞启动，且有清晰告警。

### 风险与回滚
- 风险：高频保存影响性能。
- 控制：合并写入（debounce）与脏标记。
- 回滚：关闭关键事件保存，仅保留周期保存。

## 阶段六（P2，持续）：可观测性与运维体验

### 目标
- 降低定位时长，形成“发现-定位-修复”闭环。

### 实现策略
1. 指标体系
- 核心指标：事件吞吐、同步成功率、收敛时延、补偿队列长度、GC 删除数。

2. 结构化日志标准
- 统一字段：`container_id`、`service_name`、`hostname`、`action`、`result`、`error_class`。
- 对关键流程输出摘要日志，避免只打印细节无全局视角。

3. 健康与诊断接口
- 增加“当前期望状态 vs 实际状态”诊断输出，用于快速比对漂移。

### 测试与验收
- 验收指标：
  - 故障发生后 5 分钟内可定位到模块级根因范围。
  - 告警噪音可控（误报率低）。

### 风险与回滚
- 风险：日志量膨胀影响成本。
- 控制：日志分级、采样与摘要化输出。

## 并行测试推进计划

### P0 必测
- `stop/die` 完整事件链。
- GC 与同步并发交互（死锁与竞态）。
- 退出清理路径。

### P1 必测
- 多服务容器 retention 全生命周期。
- 状态迁移与恢复。

### P2 增强
- 故障注入与补偿队列稳定性。
- 长稳压测（高频启停 + API 抖动）。

## 里程碑与交付物

### M1（阶段一完成）
- 交付物：事件可靠性改造、对账机制、P0 测试报告、回滚开关。

### M2（阶段二与三完成）
- 交付物：Retention 语义文档、状态模型升级、迁移策略、核心集成测试。

### M3（阶段四与五完成）
- 交付物：补偿队列、重试治理、持久化闭环、故障演练报告。

### M4（阶段六持续）
- 交付物：指标看板、告警规则、运维手册与排障流程。

## 建议执行顺序
1. 阶段一 + P0 测试先落地。
2. 阶段二（语义）先于阶段三（模型）执行，避免边改边返工。
3. 阶段四与五可并行推进，但先定义统一状态机与错误分类。
4. 阶段六从第一阶段开始逐步铺设，不要等到最后一次性补齐。
